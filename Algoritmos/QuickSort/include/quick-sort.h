#include <stdio.h>
#include <stdlib.h>

void swap(int*, int, int);
int DefaultQuickSort(int*, int, int, int*);
int Partition(int*, int, int);
int MedianOfThree(int, int, int);
int PartitionOptimized(int*, int, int);